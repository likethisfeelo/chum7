# CHME 유료 챌린지 · 결제/정산 기획서

- 작성일: 2026-07-21
- 상태: 초안 (v1)
- 상위 문서: `PRODUCT_SPEC.md` §8-2 (정산 실결제화)의 상세 기획
- 시스템 영향: `REDESIGN_PLAN.md`에 대한 델타는 §11에 정리
- 확정된 방향:
  - **결제 모델**: 충전식(월렛) 없이 **챌린지별 직접 결제** — 잔액 미보관으로 선불전자지급수단(전자금융거래법) 규제 회피
  - **금액 성격**: **참가비형 / 보증금형 모두 지원** (챌린지 생성 시 선택)
  - **크리에이터**: **개인·사업자 모두** 온보딩 (세무 파이프라인 분기)
  - **배송 주체**: **크리에이터 직접 발송 / 운영사 발송 모두** (챌린지별 설정)

---

## 1. 개요와 원칙

### 1.1 목표

기존의 가상 "보증금·정산(payout)" 개념을 **실결제 기반 유료 챌린지**로 승격한다.
크리에이터(권한 어드민)가 유료 챌린지를 열고, 본인인증을 마친 유저가 웹결제로 참여하며,
종료 후 크리에이터에게 정산하고, 실물 리워드가 있으면 배송까지 추적한다.

### 1.2 제품 원칙

1. **크리에이터 퍼스트** — 플랫폼 수수료는 최소한으로. 수수료 구조를 크리에이터에게 투명하게 공개하고, 정산 내역을 실시간으로 열람 가능하게 한다.
2. **돈은 단순하게** — 잔액·포인트·복잡한 프로모션 없이 "결제 → 참여 → (반환/정산)"의 직선 흐름만 만든다. 규제 표면적 최소화.
3. **신뢰가 기능보다 먼저** — 환불 규정 사전 고지, 정산 지연 없는 상태 공개, 모든 금전 이동의 감사 로그. 분쟁 시 근거가 남는 구조.
4. **무료 경험을 해치지 않는다** — 유료는 부가 레이어. 미인증/미결제 유저의 기존 무료 챌린지 경험은 그대로.

### 1.3 플랫폼의 법적 포지션 (중요)

CHME는 크리에이터와 참가자를 잇는 **통신판매중개자**로 포지셔닝한다.
- 챌린지 상세에 "판매 주체는 크리에이터이며 CHME는 중개자"임을 고지 (전자상거래법 중개자 고지 의무).
- 단, 운영사가 직접 여는 공식 유료 챌린지는 **통신판매 당사자**가 되므로 두 포지션을 구분해 표기.
- 사업 요건: **통신판매업 신고** 필요. (충전식 회피로 전자금융업 등록은 불필요, PG의 결제대행 이용)

---

## 2. 역할 모델 (권한 재정의)

| 역할 | Cognito 그룹 | 할 수 있는 것 |
|------|--------------|----------------|
| 일반 유저 | (없음) | 무료 챌린지 전부. 유료 챌린지 **열람만** 가능 |
| 인증 유저 | (없음, KYC 상태로 구분) | + 유료 챌린지 결제·참여 |
| **크리에이터** | `creators` (신설) | + 유료 챌린지 개설·운영, 자기 챌린지 정산 조회, 송장 등록 |
| 운영 어드민 | `operators` | 어드민 콘솔 일반 기능 + 커머스 조회(읽기) |
| **슈퍼 어드민** | `admins` | + 유료 챌린지 전체 관리, 환불 승인, 정산 실행, 크리에이터 승인, 수수료 설정 |

- "일부 권한을 가진 어드민" = **크리에이터 등급**으로 정의한다. 어드민 콘솔의 제한된 영역(자기 챌린지 + 정산 + 배송)만 접근.
- 크리에이터 승격은 슈퍼 어드민의 온보딩 심사(§7.2)를 통해서만 가능.

---

## 3. 유저 퍼널 — 유입부터 배송까지 (UX 흐름)

```
[발견]      마당 모집글 / 챌린지 탭 배너 / 외부 공유 링크
              │  유료 챌린지 카드: 가격·유형(참가비/보증금)·리워드 뱃지 노출
              ▼
[상세]      챌린지 상세 — 가격, 환불 규정 요약, 크리에이터 프로필, (실물) 리워드 안내
              │  비로그인 → 가입 유도 / 로그인+미인증 → "본인인증 후 참여" CTA
              ▼
[가입]      이메일 가입 (기존 흐름 그대로 — 결제 전까지 KYC 요구하지 않음)
              ▼
[본인인증]  결제 진입 시점에만 요구 (Lazy KYC)
              │  한국: 휴대폰 본인인증 (PASS/문자) → CI/DI 확보
              │  실패/외국인: "현재 한국 본인인증만 지원, 해외 인증 준비 중" 안내 (향후 카드 인증)
              ▼
[결제]      주문 요약(금액·유형·환불규정 동의 체크) → PG 웹결제창 (카드/계좌이체/간편결제)
              │  성공 → 참여 확정 + 영수증 화면 + (보증금형) "완주하면 전액 반환" 리마인드
              │  실패/이탈 → 주문 만료, 재시도 유도. 참여 확정은 웹훅 승인 후에만
              ▼
[진행]      기존 챌린지 경험 동일. 보증금형은 ME탭에 "완주 시 반환 예정 금액" 상시 노출
              ▼
[종료]      완주 판정 (기존 라이프사이클 배치)
              │  보증금형: 완주자 자동 반환(PG 취소/지급) + 알림
              │  참가비형: "수고했어요" + 리워드 안내
              ▼
[배송]      실물 리워드 대상자에게 배송지 입력 요청 (알림 + ME탭 배너, 종료 후 수집 원칙)
              │  발송 처리 → 송장 등록 → 유저는 마이페이지 [주문/배송]에서 실시간 조회
              ▼
[사후]      뱃지 후기(마당 공유) → 크리에이터의 다음 챌린지 추천 → 재참여 루프
```

**퍼널 설계 포인트**
- **Lazy KYC**: 가입 장벽을 올리지 않기 위해 본인인증은 "첫 결제 직전"에만 요구. 인증 완료율이 곧 결제 전환율이므로 인증 단계는 결제 플로우 안에 임베드(이탈 시 주문 컨텍스트 유지).
- **가격·환불 규정은 결제 전 2회 노출** (상세 + 주문 요약). 분쟁 예방의 핵심.
- **보증금형의 심리 설계**: "잃는 돈"이 아니라 "맡겨두는 돈"으로 커뮤니케이션. 참여 직후·진행 중·D6 구제 시점에 반환 조건 리마인드.
- 미인증 유저에게도 유료 챌린지를 **보여준다** (숨기지 않음) — 유료 전환 퍼널의 입구이기 때문.

---

## 4. 본인인증 (KYC)

| 항목 | 정책 |
|------|------|
| 수단 (한국) | 휴대폰 본인인증 (통신사 PASS / SMS OCR) — 본인확인기관 연동, CI/DI 수신 |
| 수단 (해외, 향후) | 카드 등록 인증(소액 승인·취소) → 국가별 인증 수단 단계 도입. v1 범위 밖, 데이터 모델만 확장 가능하게 |
| 시점 | 첫 결제 진입 시 (Lazy). 크리에이터는 온보딩 시 필수 |
| 1인 1계정 | **CI 중복 검사** — 동일 CI가 다른 계정에 이미 인증돼 있으면 차단 (다계정 어뷰징 방지) |
| 연령 | 만 14세 미만 가입 차단(법정대리인 동의 회피), 만 19세 미만 결제는 v1 차단 (미성년자 계약 취소 리스크 회피) |
| 상태 모델 | `none → pending → verified / failed` + `revoked`(운영자 회수) |
| 저장 | CI/DI·이름·생년은 **암호화 저장, 결제/정산 외 용도 사용 금지**. 탈퇴 시 관련 법정 보존 기간 후 파기 |

---

## 5. 결제

### 5.1 PG·방식

- **웹결제 전용** (앱내 결제 아님) — PWA 특성상 IAP 강제 대상이 아니며 수수료(30%)를 구조적으로 회피. 향후 네이티브 앱 출시 시에도 외부 브라우저 결제 링크 정책 유지.
- PG는 **포트원(구 아임포트) 경유**를 권장: 복수 PG 추상화·본인인증 모듈 동시 제공으로 벤더 종속 완화. (대안: 토스페이먼츠 직연동 — 정산 API가 강점. 최종 선정은 수수료 협상 후, §12 열린 질문)
- 결제 수단 v1: 신용/체크카드, 간편결제(카카오/네이버/토스), 계좌이체. 가상계좌는 환불 계좌 수집 부담으로 v2.

### 5.2 결제 흐름 (시스템 규칙)

```
① 주문 생성   POST /commerce/orders  (서버가 금액·상태 확정, 15분 만료)
② 결제창      클라이언트가 PG 위젯 호출 (주문번호·금액은 서버 발급값만 사용)
③ 승인 통지   PG 웹훅 → 서버 검증 (서명 검증 + 주문 금액 대조 + 멱등 처리)
④ 참여 확정   웹훅 승인 확정 시에만 챌린지 참여 생성 (클라이언트 성공 화면은 참고용)
⑤ 실패/만료   미승인 주문 자동 만료, 중복 승인은 멱등키로 무시
```

- **금액은 항상 서버가 결정** — 클라이언트 전달 금액 신뢰 금지, 웹훅에서 재대조.
- **원장(ledger) 기록**: 모든 금전 이벤트(승인·취소·부분취소·정산·반환)를 불변 원장에 적재. 주문 상태와 원장이 어긋나면 알람 (일 1회 대사 배치).
- 가격 정책: KRW 전용, 최소 1,000원 ~ 최대 500,000원 (슈퍼 어드민 설정값). 노출가는 부가세 포함가.

### 5.3 유형별 규칙

| | 참가비형 | 보증금형 |
|---|---|---|
| 성격 | 소멸성 — 크리에이터 수익 | 예치 — 완주 시 반환 |
| 완주 시 | 변화 없음 | **전액 반환** (PG 결제취소 우선, 기한 경과 시 계좌 지급) |
| 실패 시 | 변화 없음 | 미반환분 처리 — v1은 **크리에이터 귀속**(수수료 차감 후). "완주자 분배" 모델은 v2 검토 (§12) |
| 정산 대상 | 참가비 총액 − 수수료 | 실패자 미반환분 − 수수료 |
| 환불(중도) | §5.4 규정 | 시작 전 전액 / 시작 후 포기는 반환 없음(사전 고지) |

### 5.4 환불 정책 (전자상거래법 정합)

| 시점 | 참가비형 | 보증금형 |
|------|----------|----------|
| 결제 후 7일 이내 & 시작 전 | 전액 환불 (청약철회) | 전액 환불 |
| 시작 전 (7일 경과) | 전액 환불 (모집중 취소 자유) | 전액 환불 |
| 시작 후 | 잔여일 기준 일할 환불 − 이미 제공된 리워드 실비. 단, "개시 후 환불 불가" 고지형은 불가 | 중도 포기 = 반환 없음 (참여 시 명시 동의) |
| 챌린지 무산 (인원 미달·크리에이터 취소·운영자 중단) | **전액 환불 (자동)** | 전액 환불 (자동) |

- 환불 승인 주체: 규정 내 자동 / 규정 외 예외는 슈퍼 어드민 승인.
- 환불은 원결제 취소로만 (현금 환불·크레딧 전환 없음 — 잔액 미보관 원칙 유지).

---

## 6. 수수료와 정산 (크리에이터 퍼스트)

### 6.1 수수료 구조

```
참가자 결제액 100%
 ├─ PG 수수료 (실비, 약 2~3.5%)         → PG
 ├─ 플랫폼 수수료 (기본 5%, 설정값)      → CHME   ← "최소한의 수수료"
 └─ 크리에이터 정산액 (약 92~93%)        → 크리에이터
```

- 플랫폼 수수료율은 슈퍼 어드민이 **전역 기본값 + 크리에이터별 예외**로 설정 (프로모션 0% 가능).
- 크리에이터 대시보드에 수수료 내역을 항목별로 투명 공개.
- 구체 수수료율 숫자는 미확정 — §12 열린 질문. 기획상 상한 10% 이하 유지 권고.

### 6.2 정산 파이프라인

```
챌린지 종료 → 완주 판정 확정 → [보증금형] 완주자 반환 실행
→ 환불 보류 기간 경과 (종료 + 7일) → 정산서 생성 (금액 확정·수수료 차감)
→ 크리에이터 확인 → 슈퍼 어드민 승인 → 지급 실행 (계좌 이체) → 완료
```

- 정산 상태 머신: `pending → confirmed → approved → paid / held(보류) / disputed(이의)`
- 지급 주기: 챌린지 단위 정산 (월 배치 아님 — 소규모 크리에이터의 현금 흐름 우선). 지급일: 승인 후 영업일 3일 내.
- 지급 후 발생한 환불은 **다음 정산에서 차감** (마이너스 정산 이월).

### 6.3 세무 분기 (개인/사업자 모두 지원)

| | 개인 크리에이터 | 사업자 크리에이터 |
|---|---|---|
| 온보딩 서류 | 본인인증(KYC) + 본인 명의 계좌 (예금주 검증) | + 사업자등록증, 통신판매업 신고번호(해당 시) |
| 세무 처리 | **사업소득 원천징수 3.3%** 후 지급, 연 지급명세서 제출 | 세금계산서 수취(역발행) 기반 지급 |
| 정산서 표기 | 총액 − 플랫폼 수수료 − 원천세 = 실지급액 | 총액 − 플랫폼 수수료 (+VAT 처리) |

- 참가자 영수증: 결제 시 현금영수증/카드영수증은 PG 표준 흐름으로 발급.

---

## 7. 크리에이터 경험

### 7.1 크리에이터 대시보드 (어드민 콘솔 내 제한 영역)

- **내 유료 챌린지**: 판매 현황(결제 수/전환율/환불 수), 참가자 목록
- **정산**: 예상 정산액 실시간, 정산서 열람·확인, 지급 이력, 세무 서류
- **배송**(직접 발송형): 발송 대상 목록·배송지 다운로드, 송장 일괄 등록, 배송 현황
- 유료 챌린지 개설 폼: 가격, 유형(참가비/보증금), 환불 규정 선택, 리워드(실물 여부·발송 주체), 모집 인원 상·하한

### 7.2 크리에이터 온보딩 (슈퍼 어드민 심사)

```
신청 (프로필·계획서) → KYC + 계좌/사업자 서류 → 슈퍼 어드민 심사 (반려 사유 고지)
→ 승인: creators 그룹 부여 → 첫 챌린지는 게시 전 검수 필수, 이후 신뢰 등급별 자동 게시
```

- 어뷰징 방지 장치: **자기 챌린지 자기 결제 차단**(CI 대조), 동일 계좌 다계정 차단, 첫 정산은 소액 한도, 신고 누적 시 판매 정지 + 정산 보류.

---

## 8. 실물 리워드 배송

| 항목 | 정책 |
|------|------|
| 발송 주체 | 챌린지 설정: **크리에이터 직접** 또는 **운영사 발송** |
| 배송지 수집 | **챌린지 종료 후 대상자에게만** 수집 (개인정보 최소화 — 결제 시 수집하지 않음). 수집 기한 14일, 미입력 시 리마인드 2회 후 소멸 고지 |
| 송장 등록 | 크리에이터형: 대시보드에서 개별/CSV 일괄 등록 / 운영사형: 어드민에서 등록 |
| 배송 조회 | 택배사 조회 API(스마트택배 등) 연동 — 유저 마이페이지 [주문/배송]에서 상태 확인 |
| 상태 머신 | `주소수집중 → 발송준비 → 발송(송장) → 배송중 → 배송완료 / 반송` |
| CS | 오배송·파손·미수령 신고 → 어드민 티켓 → 발송 주체에게 라우팅. 배송 정보는 배송 완료 후 90일 보관 뒤 파기 |

---

## 9. 어드민 (커머스 운영 콘솔 — 신설 영역)

| 메뉴 | 권한 | 기능 |
|------|------|------|
| 주문/결제 | 슈퍼 (운영: 읽기) | 주문 검색·상세(원장 타임라인), 결제 상태 대사, 수동 취소 |
| 환불 | 슈퍼 | 규정 내 자동 환불 모니터, 예외 환불 승인/거절 (사유 필수) |
| 정산 | 슈퍼 | 정산서 생성 확인, 승인·지급 실행, 보류/이의 처리, 원천세·세금계산서 관리 |
| 크리에이터 | 슈퍼 | 온보딩 심사, 수수료율 예외 설정, 신뢰 등급, 판매 정지 |
| 배송 | 슈퍼/운영 | 운영사 발송 처리, 전체 배송 현황, CS 티켓 |
| KYC | 슈퍼 | 인증 현황, CI 중복 탐지, 인증 회수 |
| 리포트 | 슈퍼/운영 | 일/월 거래액·환불률·정산 예정액, 대사 불일치 알람 |
| 감사 로그 | 슈퍼 | 모든 금전 관련 어드민 행위 기록 (누가·언제·무엇을·사유) — 기존 payout 감사 로그 확장 |

---

## 10. 이 외에 신경써야 할 부분 (리스크 체크리스트)

1. **대사(reconciliation)** — PG 정산 파일과 내부 원장의 일 배치 대조. 불일치 시 정산 중단 알람. 결제 시스템에서 가장 흔히 빠뜨리는 부분.
2. **웹훅 신뢰성** — PG 웹훅 유실 대비 폴링 보정(미확정 주문 상태 재조회), 중복 수신 멱등 처리.
3. **부분 실패 시나리오** — 결제 성공 후 참여 생성 실패(정원 초과 경합 등) → 자동 전액 취소 + 알림. "돈만 나가고 참여 안 됨"은 최악의 신뢰 사고.
4. **모집 정원과 결제 경합** — 정원 임박 시 좌석 선점(주문 생성 시 10분 홀드) 도입.
5. **분쟁·차지백** — 카드사 이의제기 대응 프로세스(증빙: 참여 로그·고지 동의 기록). 차지백 발생 시 해당 정산 자동 보류.
6. **크리에이터 리스크** — 챌린지 무산·운영 방치 시 자동 전액 환불 규정이 크리에이터 평판과 연결되도록 (무산 이력 공개).
7. **개인정보 등급 상승** — CI·계좌·배송지 취급으로 개인정보처리방침 개정, 수탁사(PG·본인확인기관·택배 조회) 고지, 암호화·접근 통제(어드민도 마스킹 기본).
8. **가격 표시·광고 규제** — "완주하면 무료" 류 표현은 보증금형에서만, 조건 병기 필수.
9. **회계 기초** — 예치금(보증금형)은 부채로, 수수료만 수익으로 인식. 정산 전 크리에이터 몫은 회사 돈이 아님을 장부에서 구분 (별도 계좌 운용 권장).
10. **고객센터 동선** — 결제·환불·배송 문의 채널과 응답 SLA. 유료화 순간부터 CS는 기능이 아니라 의무.

---

## 11. 시스템 영향 (REDESIGN_PLAN 델타)

재구축 계획에 다음을 추가한다:

- **서비스 +1**: `commerce-api` (주문/결제/환불/정산/배송/KYC). PG 웹훅 수신은 **인증 없는 퍼블릭 엔드포인트 + 서명 검증**으로 별도 라우트 분리.
- **워커 +2**: `settlement-worker` (반환·정산서 생성·대사 배치), `shipping-tracker` (배송 상태 폴링).
- **테이블 +1**: `commerce` (orders / payments / refunds / ledger-entries / settlements / creator-profiles / kyc-records / shipments — 단일 바운디드 컨텍스트). 원장은 append-only.
- **시크릿 관리**: PG API 키·본인확인 키는 Secrets Manager (stages.ts 하드코딩 금지 원칙 준수).
- **admin-api 확장**: §9 커머스 콘솔. 크리에이터 제한 영역은 `creators` 그룹 클레임으로 미들웨어 격리.
- **기존 payout 개념 대체**: 재구축 시 가상 payout·payout-audit-logs는 이 문서의 정산·감사 체계로 흡수 (별도 이관 없음 — 데이터 새 출발).
- **역할 그룹 확정**: `admins / operators / creators` (REDESIGN_PLAN §5.3의 `leaders` 임시안을 `creators`로 대체).

## 12. 열린 질문 (구현 전 확정 필요)

1. **플랫폼 수수료율** 기본값 (권고: 5% 내외, 상한 10%).
2. **보증금형 실패분 귀속** — v1 크리에이터 귀속으로 가나, "완주자 분배(상금 풀)" 모델 도입 여부. 분배 모델은 사행성 판단 리스크 검토 필요.
3. **PG 최종 선정** — 포트원 vs 토스페이먼츠 직연동 (수수료·정산 API·본인인증 패키지 비교 견적).
4. **시작 후 참가비 환불 규정** — 일할 환불 허용 vs 개시 후 불가 고지형, 크리에이터 선택제로 둘지.
5. **미성년자(만 19세 미만) 결제** — v1 차단안 유지 여부.
6. **운영사 발송형의 물류** — 3PL 위탁 여부 (v1은 수동 처리 가능한 물량 가정).

## 13. 단계별 로드맵

| 단계 | 범위 | 비고 |
|------|------|------|
| **M1** | KYC(한국) + 참가비형 결제 + 규정 내 환불 + 수동 정산(어드민 지급 처리) + 원장·대사 | 최소 유료화. 재구축 Phase 2와 병행 가능 |
| **M2** | 크리에이터 온보딩·대시보드 + 정산 자동화(원천징수/세금계산서) + 커머스 어드민 완성 | |
| **M3** | 보증금형 (완주 반환 자동화) | 기존 payout 개념의 실결제 대체 |
| **M4** | 실물 리워드 배송 (양 발송 주체 + 조회 연동) | |
| **M5** | 해외 유저 (카드 인증, 다통화·해외 PG) | 별도 기획 |
