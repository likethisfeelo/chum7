{
  "name": "chum7",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*",
    "services/workers/*",
    "infra2"
  ],
  "scripts": {
    "test": "jest",
    "typecheck": "tsc -p packages/contracts && tsc -p packages/core && tsc -p packages/api-kit && tsc -p packages/web-kit && tsc -p services/user-api && tsc -p services/challenge-api && tsc -p services/social-api && tsc -p services/cheer-api && tsc -p services/gamification-api && tsc -p services/commerce-api && tsc -p services/admin-api && tsc -p services/chat-api && tsc -p services/workers/notification-worker && tsc -p services/workers/cheer-scheduler && tsc -p services/workers/lifecycle-manager && tsc -p services/workers/plaza-converter && tsc -p services/workers/settlement-worker && tsc -p services/workers/interaction-projector && tsc -p infra2",
    "deploy": "node scripts/deploy.mjs",
    "ops:set-vapid": "node scripts/ops/set-vapid.mjs",
    "ops:set-anon-salt": "node scripts/ops/set-anon-salt.mjs",
    "ops:set-oauth": "node scripts/ops/set-oauth.mjs",
    "backfill:challenge-layer-policy": "node scripts/backfill-challenge-layer-policy.mjs"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.7.3"
  }
}
