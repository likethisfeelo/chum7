# CHME 서비스 기획서 (제품 정의서)

- 작성일: 2026-07-21 (v2)
- 상태: **v2 — 유료 챌린지·크리에이터 체계 통합판**
- v1 → v2 변경: ① 결제·정산·배송을 1급 기능으로 통합 (`PAYMENT_SPEC.md`가 상세 부속 문서)
  ② 리더/크리에이터 도구 고도화 스펙 신설 (§4.12) ③ 중복 화면 정리 확정 (§3.1)
  ④ 탐색 고도화 확정 (§4.2) ⑤ 푸시 알림 확정 (§4.10)
- 자매 문서: `REDESIGN_PLAN.md` (시스템 재구축 — 이 문서가 기능 요건 기준),
  `PAYMENT_SPEC.md` (결제·정산·배송 상세)

---

## 1. 서비스 개요

### 1.1 한 줄 정의

> **CHME (Challenge Earth with ME, 챌린지어스미)** — 7일 단위 챌린지로 습관을 만들고
> 참가자들이 서로의 실천을 응원으로 밀어주는 **소셜 습관 형성 서비스**이자,
> 검증된 리더가 유료 챌린지를 열어 수익을 만드는 **크리에이터 챌린지 플랫폼**.

- 도메인: www.chum7.com (사용자), admin.chum7.com (운영자·크리에이터 콘솔)
- 형태: 모바일 우선 웹앱(PWA, 홈 화면 설치·웹 푸시 지원) + 콘솔 웹앱

### 1.2 해결하려는 문제

| 대상 | 문제 | CHME의 접근 |
|------|------|-------------|
| 참가자 | 혼자 하는 습관 앱은 지쳐서 이탈 | 같은 챌린지 동료의 인증이 보이고, **응원이 내 실천 시각에 맞춰 도착** |
| 참가자 | 완벽하지 못하면 포기하는 심리 | 부분 완료(partial)·**Day 6 보완 인증(구제)**·추가 기록으로 "덜 완벽한 실천"을 수용 |
| 참가자 | 기록이 쌓여도 보람이 없음 | 인증이 **마당(공개 피드)** 에 흐르고 캐릭터·뱃지·세계로 보상이 누적 |
| 리더 | 모임 운영이 수작업 (단톡방·엑셀) | 퀘스트 출제·심사·공지·통계·리마인드까지 **리더 운영 도구** 제공 |
| 크리에이터 | 습관 콘텐츠로 수익화할 채널 부재 | **유료 챌린지 + 최소 수수료 정산 (크리에이터 퍼스트)** |

### 1.3 제품 원칙

1. **작게, 7일씩** — 부담 없는 단위로 시작하고 반복한다.
2. **혼자 하지 않게** — 모든 실천은 누군가에게 보이고 응원받을 수 있다.
3. **실패에 관대하게** — partial·구제·추가 기록 등 불완전한 실천도 제품이 수용한다.
4. **실천이 곧 콘텐츠** — 별도 글쓰기 없이 인증 자체가 피드가 된다.
5. **크리에이터 퍼스트** — 수수료는 최소로, 정산은 투명하게, 도구는 아끼지 않는다.
6. **돈은 단순하게** — 잔액·포인트 없이 "결제 → 참여 → 반환/정산"의 직선 흐름만.

---

## 2. 사용자 정의 — 성장 사다리

사용자는 하나의 계정으로 아래 사다리를 오른다. 단계 전환이 제품의 핵심 성장 루프다.

```
참가자 ──(챌린지 개설)──▶ 리더 ──(온보딩 심사)──▶ 크리에이터
  │                        │                        │
  무료·유료 참여            무료 챌린지 운영          유료 챌린지 판매·정산
```

| 사용자 | 정의 | 권한 근거 |
|--------|------|-----------|
| **참가자** | 챌린지에 참여해 매일 인증하는 사용자. 이메일 가입, 유료 참여 시에만 본인인증(Lazy KYC) | 기본 |
| **리더** | 무료 챌린지를 개설·운영하는 사용자. 퀘스트 출제·심사, 게시판, 참가 관리 | 자기 챌린지 한정 (별도 승인 불요) |
| **크리에이터** | 온보딩 심사를 통과해 **유료 챌린지**를 열 수 있는 리더. 정산·배송 도구 접근 | Cognito 그룹 `creators` |
| **운영자** | 서비스 운영팀. 공식 챌린지·심사·모니터링 | 그룹 `operators` |
| **슈퍼 어드민** | 환불 승인·정산 실행·크리에이터 심사·수수료 설정 등 금전 권한 | 그룹 `admins` |

- 계정: 이메일+비밀번호(이메일 인증 필수), 비밀번호 재설정.
- 프로필: 닉네임, 이미지, **피드 핸들(@아이디)**, 정체성 문구.
- 본인인증(KYC): 결제 진입 시 요구. 한국 휴대폰 본인인증(CI 기반 1인 1계정), 해외는 카드 인증 확장 예정 — 상세 `PAYMENT_SPEC.md` §4.

---

## 3. 정보 구조 (IA)

### 3.1 사용자 앱 — 하단 5탭 (중복 화면 정리 확정)

```
┌─────────┬─────────┬────────┬─────────┬────────┐
│ 챌린지 🎯│  마당 🚀 │  ME    │ 오늘 📊  │ 마이 ✨ │
└─────────┴─────────┴────────┴─────────┴────────┘
```

| 탭 | 역할 (경계 확정) |
|----|------------------|
| **챌린지** | 탐색 전용 — 검색·필터·추천·배너, 상세, 참여(결제 포함), 개설 |
| **마당** | 공개 소셜 피드 — 인증글·모집글·소식·뱃지 후기, 해시태그 |
| **ME** | **실천 허브** — 오늘 인증할 것(폼 즉시 노출), 진행중/완주 챌린지, 내 기록·추가기록. "행동하는 곳" |
| **오늘** | 세계(월드) — 전체 사용자의 오늘 실천 8층 시각화. "동기 받는 곳" |
| **마이** | **계정 허브** — 프로필 관리, 뱃지·캐릭터 컬렉션, **주문/배송**, 응원함, 알림, 설정. "관리하는 곳" |

**중복 화면 정리 결정** (재구축 시 확정 적용):

| 기존 | 결정 |
|------|------|
| `/me` vs `/my` | **둘 다 유지하되 경계 확정**: ME = 실천(오늘 할 일·기록), 마이 = 계정(프로필·컬렉션·주문·설정). 겹치던 요소는 위 표 기준으로 한쪽에만 배치 |
| `/profile` vs `/assets` | **폐지 → 통합**: 내 공개 프로필은 `/@핸들`(개인 피드)로 **단일화**. 프로필 편집·뱃지·캐릭터는 마이 탭 내부로. `/assets` 삭제 |
| `/earth` 리다이렉트 | 삭제 (`/outer-space`→ 신규 경로 `/plaza`로 개명) |
| `/today/debug`, `/design-mockup/*`, `/ux-plan` | 삭제 (내부용 화면은 앱 라우터에서 제거) |
| 사용자 앱 내 `/admin/*` | 삭제 — 관리자 표면은 admin 앱으로 전부 이동 |

### 3.2 콘솔 앱 (admin.chum7.com) — 역할별 3영역

```
크리에이터 영역 (creators)   내 챌린지 운영 · 판매 현황 · 정산 · 배송
운영자 영역   (operators)   대시보드 · 챌린지/퀘스트 관리 · 배너 · 모니터
슈퍼 영역     (admins)      환불 승인 · 정산 실행 · 크리에이터 심사 · 수수료 · 감사 로그
```

리더(무료)의 운영 도구는 **사용자 앱 내부**(챌린지별 "운영" 탭)에 둔다 — 리더는 콘솔 진입 없이 모바일에서 운영 (§4.12).

---

## 4. 기능 명세

### 4.1 챌린지 (핵심 도메인)

**개념**: 기간(기본 7일)·카테고리·목표 시각을 가진 실천 프로그램.

**2축 유형 체계** (생성 시 선택):

- **인증 축 `challengeType`** — 하루 완료 기준:

| 유형 | 하루 완료 조건 | 개인 퀘스트 |
|------|----------------|-------------|
| `leader_only` 리더 퀘스트형 | 리더 퀘스트 인증 1건 | 없음 |
| `personal_only` 개인 퀘스트형 | 개인 퀘스트 인증 1건 | 참여 시 필수 |
| `leader_personal` 혼합형 | 리더 **AND** 개인 모두 | 참여 시 필수 |

  혼합형에서 한쪽만 인증하면 `partial`, 채우면 `success`.

- **가격 축 `pricingType`** — 참여 조건과 돈의 흐름:

| 유형 | 개설 권한 | 규칙 |
|------|-----------|------|
| `free` 무료 | 모든 사용자(리더) | 현행과 동일 |
| `paid_fee` 참가비형 | 크리에이터·운영자 | 소멸성 참가비 → 크리에이터 수익 |
| `paid_deposit` 보증금형 | 크리에이터·운영자 | 완주 시 전액 반환, 실패분은 수수료 차감 후 크리에이터 귀속 |

  결제·환불·정산 규칙 상세는 `PAYMENT_SPEC.md` §5~6.

**라이프사이클** (시간별 자동 전환): `draft → recruiting → preparing → active → completed → archived`.
완주 판정·뱃지·캐릭터 진행도·**보증금 반환·정산 개시**가 종료 시점에 자동 연쇄된다.
사용자 화면은 4버킷(진행중/준비중/완주/기타)으로 단순화.

**참여**: 참여 마법사(바텀시트) — 유형에 따라 개인 목표 입력 필수(`layerPolicy`), 유료면 KYC→결제 단계 임베드.
리더 승인형은 신청→승인. 중도 포기 가능(유료는 환불 규정 연동). 시즌제 재개설 지원(§4.12).

### 4.2 탐색 (고도화 확정)

챌린지 탭을 "목록"에서 "발견 엔진"으로 격상한다. v1은 전부 **룰 기반**(ML 없음).

| 요소 | 스펙 |
|------|------|
| **검색** | 챌린지 제목·소개·해시태그·크리에이터 핸들 통합 검색. 최근 검색어 저장(로컬). v1은 프리픽스+해시태그 매칭, 검색 고도화(형태소·오타)는 OpenSearch 도입 시점에 (백로그) |
| **필터** | 카테고리 / 인증 유형 / 가격(무료·유료) / 상태(모집중·시작 임박·상시) |
| **정렬** | 추천(기본) · 최신 · 인기(참여자 수) · 마감 임박 · 낮은 가격순 |
| **추천 섹션** (홈 큐레이션, 위→아래) | ① 마감 임박 (모집 마감 D-1~2) ② 내 카테고리 신규 (참여 이력 기반) ③ 팔로우한 크리에이터의 새 챌린지 ④ 지금 인기 (7일 참여 증가율) ⑤ 처음이라면 (신규 유저 온보딩 추천 — 무료·입문 태그) |
| **배너** | 현행 카테고리 배너 유지 (운영 큐레이션) |
| 노출 정책 | 유료 챌린지는 미인증 유저에게도 노출 (전환 퍼널 입구). 가격·유형 뱃지 카드에 상시 표기 |

### 4.3 인증 (일일 실천 기록)

| 정책 | 내용 |
|------|------|
| 유형 | 사진 / 텍스트 / 링크 / 동영상(트림 지원, 업로드 전 미디어 검증) |
| 시각 | `performedAt`(사용자 선택, 당일·과거만) vs `uploadAt`(서버 수신) 이원화. 보상 판정은 uploadAt |
| Day 계산 | 시작일 기준 캘린더 계산(Day X/Y), 사용자 타임존 지원 |
| 보완 인증(구제) | Day 6 한정, Day 1~5 실패분 만회 (`remedied`) |
| 추가 기록 | 하루 완료 후 초과 인증. 기본 나만 보기, ME탭에서 공개 전환 |
| 공개 범위 | 인증별 공개/비공개 + 익명 옵션. 공개 인증만 마당 전환 |

### 4.4 응원 (소셜 동기부여 핵심)

- 인증 후 같은 챌린지 동료(그룹 매칭)에게 응원 → **상대의 목표 실천 시각에 맞춰 예약 도착** (5분 주기 발송).
- 발송 시점에 수신자가 이미 완료했다면 알림 대신 **발신자에게 감사 점수(thankScore)** 적립 (점수와 알림의 분리 원칙).
- 이모지 리액션·답장, 응원함(마이 탭), 레이트 리밋, 실패분 데드레터 재처리, 사용자별 통계 집계.
- **응원 도착은 푸시 알림 최우선 유형** (§4.10) — "내 실천 시각에 응원이 도착하는" 경험은 푸시가 있어야 완성된다.

### 4.5 마당 (공개 소셜 피드)

- 공개 인증이 시간별 배치로 **자동 전환**되어 흐르는 광장. 경로 `/plaza`로 개명.
- 게시물 유형: 인증글(자동), 모집글(챌린지 홍보 — 유료 챌린지 판매 동선), 진행 소식, 뱃지 후기.
- 상호작용: 댓글·이모지 리액션·저장·추천(기간 만료형, 닫기 가능)·해시태그(팔로우, 태그 피드).

### 4.6 챌린지 내부 커뮤니티

| 공간 | 성격 | 주요 기능 |
|------|------|-----------|
| 챌린지 피드 | 참가자 인증 스트림 | 인증 카드 리액션·댓글, 익명 ID, 리더 DM |
| 챌린지 게시판 | 리더 콘텐츠 보드 | 블록 에디터(텍스트/이미지/링크/인용/동영상), 참여 전 프리뷰 보드, 인용 댓글 |
| 불레틴 | 단계별 가벼운 게시판 | 글/댓글/좋아요 |

### 4.7 퀘스트

- **리더 퀘스트**: 리더 출제 일일 과제 → 제출 → 리더/운영자 심사. 이력 전량 보존, 중복 제출 차단. 템플릿·예약 출제는 §4.12.
- **개인 퀘스트**: 참여 시 스스로 설정. 신규 제안은 운영자 심사.

### 4.8 게이미피케이션

| 장치 | 내용 |
|------|------|
| 캐릭터(신화) | 온보딩에서 선택, 완주가 진행도·슬롯 해금, 선택 캐릭터가 앱 테마 결정 |
| 뱃지 | 완주 뱃지 + 리더 뱃지, 마이 탭 컬렉션, 마당 후기 공유 |
| 점수 | `score`(완료 일수) + `thankScore`(응원 감사) |
| 세계(오늘 탭) | 오늘(KST) 전체 실천을 B2~G6 8층 구조물로 시각화 |

### 4.9 개인 피드 (프로필 소셜)

- `/@핸들` — 유일한 공개 프로필 (§3.1 통합 결정). 인증탭/자유탭 3열 그리드.
- 팔로우(비공개 계정은 요청·승인), 차단, 만료형 초대 링크, 자유글, 저장 목록.
- 크리에이터의 개인 피드에는 **진행중·모집중 챌린지 섹션** 노출 (판매 동선).

### 4.10 알림 — 인앱 + 웹 푸시 (확정)

**푸시 채널**: PWA **Web Push**(VAPID). 이미 설치형 PWA이므로 추가 앱 없이 지원.
iOS는 홈 화면 추가 시 지원(16.4+) — 미설치 iOS 사용자에게는 설치 유도 배너로 안내.

| 항목 | 정책 |
|------|------|
| 권한 요청 시점 | 가입 직후가 아니라 **첫 응원 예약 완료 직후** — "응원이 도착하면 알려드릴까요?" (수락률 최적화) |
| 우선순위 | ① 응원 도착 ② 리더 공지·리마인드 ③ 결제·반환·정산·배송 ④ 댓글·리액션 ⑤ 팔로우 ⑥ 운영 공지 |
| 설정 | 기존 카테고리 ON/OFF와 통합 (인앱/푸시 채널별 토글). 방해금지 시간(기본 22–08시) — 단 응원 도착은 "실천 시각 도착"이 본질이므로 방해금지 예외 옵션 제공(기본 ON) |
| 딥링크 | 모든 푸시는 해당 화면으로 직행 (응원함, 챌린지 피드, 주문 상세 등) |
| 발송 구조 | 도메인 이벤트(EventBridge) → 알림 워커가 구독 → 수신 설정 확인 → 인앱 기록 + 푸시 발송. 인앱과 푸시가 한 파이프라인에서 갈라진다 (재구축 시 이벤트 버스 실사용 전환 — REDESIGN_PLAN §3.2) |
| 집계 | 같은 게시물 다중 리액션은 묶음 발송 ("OO 외 3명이 반응"), 폭주 방지 |

### 4.11 결제 · 정산 · 배송 (요약 — 상세는 PAYMENT_SPEC.md)

- **직접 결제** (월렛 없음): 주문 → PG 웹결제 → 웹훅 승인 시에만 참여 확정. Lazy KYC.
- **환불**: 시작 전 전액 / 시작 후 규정별 (전자상거래법 정합). 무산 시 자동 전액 환불.
- **정산**: 챌린지 단위, 종료+7일 보류 후 지급. 플랫폼 수수료 최소(기본 5% 내외, 설정값). 개인(3.3% 원천징수)/사업자(세금계산서) 분기.
- **배송**: 종료 후 대상자에게만 배송지 수집, 크리에이터 직접/운영사 발송, 송장 등록 → 마이 탭 [주문/배송]에서 조회.
- 플랫폼 포지션: 통신판매**중개자** (크리에이터 챌린지) + 당사자 (공식 챌린지).

### 4.12 리더 · 크리에이터 도구 (고도화 — 신규 상세)

**설계 사상**: 리더의 하루를 "확인 → 독려 → 심사" 3동작으로 압축한다. 리더 도구는
**사용자 앱의 챌린지 내부 "운영" 탭**(리더에게만 노출)에 두어 모바일에서 즉시 처리하게 하고,
크리에이터의 돈·판매 관련 기능만 콘솔에 둔다.

#### A. 리더 운영 탭 (사용자 앱 내, 무료·유료 공통)

| 기능 | 스펙 |
|------|------|
| **오늘 브리핑** | 오늘 인증률(n/m), 미인증자 목록, 심사 대기 제출물 수, 신규 참가 신청 수 — "오늘 할 일" 카드 1장 |
| **참가자 관리** | 목록(진행률·연속 미인증·구제 사용 여부), 참여 신청 승인/거절, **경고 → 내보내기(강퇴)** 2단계 (유료는 환불 규정 자동 연동 + 사유 기록 필수), 참가자별 메모 |
| **독려 도구** | 미인증자 대상 **리마인드**: 수동 1탭 발송 + 자동 규칙(예: 20시까지 미인증 시 자동, 리더 설정) — 일 1회 제한, 푸시 ② 순위로 발송 |
| **공지** | 게시판 핀 고정 + 전체 알림 발송(일 1회 제한), 예약 공지 |
| **퀘스트 운영** | 퀘스트 **템플릿 저장·재사용**, 7일치 **예약 출제**(D1~D7 미리 등록), 제출물 심사 인박스(승인/반려 스와이프), 반려 사유 프리셋 |
| **통계** | 일별 인증률 추이 그래프, 완주율, 응원 활동량, 시간대별 인증 분포(리마인드 시각 최적화 근거), **기수 간 비교**(시즌제) |
| **시즌제 재개설** | 종료 챌린지 **복제 → 다음 기수 개설** (설정·퀘스트 템플릿 승계), 이전 기수 참가자 우선 초대(알림+초대 링크) |

#### B. 크리에이터 콘솔 (admin 앱 내 제한 영역, `creators` 그룹)

| 기능 | 스펙 |
|------|------|
| 판매 현황 | 챌린지별 결제 수·전환율(상세 조회→결제)·환불 수·순매출 |
| 정산 | 예상 정산액 실시간, 정산서 확인, 지급 이력, 세무 서류(원천징수영수증/세금계산서) |
| 배송 | 발송 대상·배송지 다운로드, 송장 개별/CSV 등록, 배송 현황 |
| 유료 개설 폼 | 가격·유형(참가비/보증금)·환불 규정 선택·리워드(실물 여부·발송 주체)·정원 상하한 |
| 프로필 | 크리에이터 소개 페이지 (개인 피드와 연결), 무산 이력 등 신뢰 지표 공개 |

#### C. 성장 루프 (제품 관점)

```
참가자가 무료 챌린지 개설(리더) → 운영 탭으로 낮은 부담 운영 경험
→ 완주율·기수 실적이 쌓임 → "크리에이터 신청" CTA (운영 탭 내 노출)
→ 온보딩 심사 통과 → 같은 챌린지를 유료 기수로 전환 (시즌제 복제 + 가격 설정)
```
리더 도구가 곧 크리에이터 파이프라인이다 — 무료 리더 경험의 품질이 유료 공급을 만든다.

### 4.13 운영자 콘솔 (operators / admins)

| 메뉴 | 기능 |
|------|------|
| 대시보드 | 사용자·챌린지·인증 지표 + **거래액·환불률·정산 예정액** |
| 챌린지 관리 | 공식 챌린지 생성·수정, 라이프사이클 강제 전환, 응급 전체 조회 |
| 심사 | 퀘스트 제출물, 개인 퀘스트 제안, **크리에이터 온보딩**(슈퍼), 유료 챌린지 게시 전 검수 |
| 커머스 (슈퍼) | 주문/결제 대사, 예외 환불 승인, 정산 승인·지급, 수수료 설정, KYC 관리 |
| 배송 | 운영사 발송 처리, 전체 현황, CS 티켓 |
| 콘텐츠 | 카테고리 배너, 추천 섹션 큐레이션(§4.2 ⑤ "처음이라면" 수동 편성) |
| 모니터 | 응원 발송·데드레터 재처리, 알림 발송 현황(푸시 성공률) |
| 감사 로그 | 금전·권한 관련 모든 어드민 행위 기록 |

---

## 5. 핵심 사용자 여정

### 5.1 참가자의 7일 (유료 보증금형)

```
D-5  마당 모집글 → 상세(가격·"완주하면 전액 반환"·환불 규정) → 본인인증 → 웹결제 → 준비중
D-1  푸시: "내일 시작해요" (리더 공지)
D1   ME탭 인증 → 응원 예약 → 푸시 권한 요청("응원이 도착하면 알려드릴까요?")
D2   목표 시각에 응원 푸시 도착 → 앱 복귀 → 인증 (핵심 리텐션 루프)
D5   하루 놓침 → 저녁 리더 리마인드 푸시
D6   보완 인증으로 D5 만회 (반환 조건 리마인드 노출)
D7   완주 → 자동 판정 → 보증금 전액 반환 (푸시+알림) → 뱃지·캐릭터 진행
D+2  실물 리워드 배송지 입력 요청 → 배송 조회 → 뱃지 후기 마당 공유 → 다음 챌린지 추천
```

### 5.2 리더 → 크리에이터 성장 여정

```
무료 챌린지 개설 → 운영 탭에서 기수 운영 (브리핑·리마인드·심사)
→ 2기 시즌제 복제 (참가자 재초대) → 완주율 실적 누적
→ "크리에이터 신청" → KYC+계좌/사업자 온보딩 → 슈퍼 어드민 심사 승인
→ 3기를 참가비형으로 전환 (첫 챌린지 게시 전 검수) → 판매 → 종료+7일 → 정산 지급
→ 실물 리워드 발송 (송장 등록) → 크리에이터 프로필에 실적 공개 → 팔로워에게 신작 푸시
```

---

## 6. 정책 요약표

| 영역 | 정책 |
|------|------|
| 하루의 기준 | 사용자 타임존 캘린더 일 (세계 집계만 KST) |
| 인증 마감 | 당일 23:59, 실천 시각은 당일 내 소급 선택, 미래 불가 |
| 구제 | Day 6 한정, Day 1~5 실패분 |
| 공개 기본값 | 정규 인증: 챌린지 설정 / 추가 기록: 나만 보기 |
| 마당 전환 | 공개 인증만, 시간별 자동 배치 |
| 응원 도착 | 목표 시각 예약·5분 발송, 완료자에겐 미발송+발신자 점수, 푸시 최우선 |
| 결제 | 직접 결제만(잔액 없음), 웹훅 승인 후 참여 확정, 금액은 서버 확정 |
| 환불 | 시작 전 전액 / 시작 후 유형별 규정 / 무산 시 자동 전액 |
| 정산 | 챌린지 단위, 종료+7일 후, 수수료 투명 공개, 개인/사업자 세무 분기 |
| 배송지 | 종료 후 대상자에게만 수집, 배송 완료 90일 후 파기 |
| 계정 공개성 | `/@핸들` 공개/비공개, 비공개는 팔로우 승인제 |
| 권한 사다리 | 참가자 < 리더(자기 챌린지) < 크리에이터(+유료·정산) < 운영자 < 슈퍼 어드민 |
| 푸시 | 첫 응원 예약 후 권한 요청, 카테고리·채널별 설정, 방해금지(응원 예외 옵션) |

## 7. 용어집

| 용어 | 정의 |
|------|------|
| 마당 | 서비스 전체 공개 피드 (`/plaza`) |
| 인증 | 일일 실천 증빙 제출 (verification) |
| 구제 / 보완 인증 | Day 6에 과거 실패일을 만회하는 인증 (remedy) |
| 추가 기록 | 하루 완료 후의 초과 인증 (extra) |
| 응원 | 동료의 실천 시각에 맞춰 예약 도착하는 격려 (cheer) |
| 감사 점수 | 완료자에게 향한 응원의 발신자가 받는 점수 (thankScore) |
| 리더 / 크리에이터 | 무료 챌린지 운영자 / 유료 판매 권한을 승인받은 리더 |
| 참가비형 / 보증금형 | 소멸성 결제 / 완주 시 전액 반환 결제 |
| 시즌제(기수) | 종료 챌린지를 복제해 반복 개설하는 운영 방식 |
| 세계 | 오늘 탭의 8층 실천 시각화 (world) |
| 운영 탭 | 챌린지 내부의 리더 전용 관리 화면 |
| 버킷 | 사용자 화면용 챌린지 상태 4분류 |

## 8. 백로그 (v2 이후)

1. **해외 유저** — 카드 인증·다통화·해외 PG (`PAYMENT_SPEC` M5).
2. **탐색 지능화** — OpenSearch 기반 검색 고도화, 행동 로그 기반 개인화 추천 (v1 룰 기반의 다음 단계).
3. **보증금 완주자 분배(상금 풀)** — 사행성 규제 검토 선행 (`PAYMENT_SPEC` §12-2).
4. **네이티브 앱** — PWA 한계(iOS 푸시 미설치 유저) 보완 시점에 검토, 결제는 외부 웹결제 유지.
5. **온보딩 퍼널 정교화** — 가입→첫 참여 전환 실험 (추천 섹션 ⑤가 v1 대응).
6. **리더 도구 확장** — 참가자 코호트 리포트 자동 발송, 운영 어시스턴트(자동 응원·자동 피드백).
